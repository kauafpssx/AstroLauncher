# CodeRabbit — automated AI review on PRs.
# ⚠️ Requires installing the GitHub App: https://github.com/apps/coderabbitai
# (free for public repos). Without the app, this file is ignored.
# Docs: https://docs.coderabbit.ai/guides/configure-coderabbit
language: pt-BR
reviews:
  profile: assertive
  request_changes_workflow: false
  high_level_summary: true
  poem: false
  review_status: true
  collapse_walkthrough: false
  auto_review:
    enabled: true
    drafts: false
  path_filters:
    - '!**/*.lock'
    - '!**/node_modules/**'
    - '!src-tauri/target/**'
    - '!dist/**'
  tools:
    ruff:
      enabled: false
    markdownlint:
      enabled: true
    gitleaks:
      enabled: true
    eslint:
      enabled: true
    clippy:
      enabled: true
chat:
  auto_reply: true
